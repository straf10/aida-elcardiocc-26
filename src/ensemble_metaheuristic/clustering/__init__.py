"""
Clustering-based routing (score-matrix features, text embeddings).

Modules: :mod:`embeddings`, :mod:`score_matrix`, :mod:`text`.
For a flat import surface, use ``from ensemble_metaheuristic.strategies import …`` (lazy re-exports).
"""
