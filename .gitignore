# Secrets (do not commit)
.env

# Cursor IDE personal config
.cursorrules

# Python virtual environment
.venv/
venv/
__pycache__/
*.py[cod]
*$py.class

# Setuptools / pip (editable install metadata, wheels, sdist)
*.egg-info/
dist/
build/
*.egg

# DS Store
.DS_Store

# Analysis outputs for xlm_r_base (contaminated val/train overlap — excluded from REPORT.md)
outputs/evaluation/xlm_r_base/

# Trial run artifacts (local experiments, not needed in repo)
trial_runs/

# Local training logs under src (should not be committed)
src/xlm_r_base/train_v15_base.log

# Weights & Biases local run data (runs sync to wandb.ai; not needed in repo)
wandb/
data/processed/_augmented/

# Local comparison notes (optional; remove from gitignore if you want to track it)
XLM_R_LARGE_VS_BASE.md

# Private dictionary refactor notes (local only)
MEMORY.md

# Large model checkpoints (tracked paths only; other files under outputs/models/ may still be committed)
outputs/models/mlc_greek_bert/best_model.pt
outputs/models/greek_bert/best_model.pt
outputs/models/ner_el/model.safetensors
outputs/models/ner/model.safetensors
outputs/models/xlm_r_base/model_fold_4.pt
outputs/models/xlm_base/model_fold_4.pt
outputs/models/xlm_r_large/model.safetensors
outputs/models/xlm_large/model.safetensors
